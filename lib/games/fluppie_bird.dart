import 'package:flutter/material.dart';
class FlappySquareScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Flappy Square")),
      body: Center(child: Text("Flappy Square Game Here")),
    );
  }
}