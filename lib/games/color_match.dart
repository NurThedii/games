import 'package:flutter/material.dart';

class ColorMatchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Color Match")),
      body: Center(child: Text("Color Match Game Here")),
    );
  }
}
